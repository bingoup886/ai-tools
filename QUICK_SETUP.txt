═══════════════════════════════════════════════════════════════
  Cloudflare Pages 快速配置 - 一页纸指南
═══════════════════════════════════════════════════════════════

🔑 关键问题和答案：

Q: 预设框架选什么？
A: 选 "无" 或 "Custom" (不要选 React)

Q: Build command 填什么？
A: npm run build

Q: Build output directory 填什么？
A: dist

Q: Root directory 填什么？
A: / (或留空)

═══════════════════════════════════════════════════════════════

📋 完整步骤：

1. 打开 Cloudflare Dashboard
   https://dash.cloudflare.com/

2. Pages → ai-tools-34x → Settings

3. 找到 "Build and deployments" 部分

4. 设置以下字段：

   ┌─ Framework preset ────────────┐
   │ 无 / None / Custom            │
   └───────────────────────────────┘

   ┌─ Build command ───────────────┐
   │ npm run build                 │
   └───────────────────────────────┘

   ┌─ Build output directory ──────┐
   │ dist                          │
   └───────────────────────────────┘

   ┌─ Root directory ──────────────┐
   │ / (或留空)                    │
   └───────────────────────────────┘

5. 点击 "Save" 保存

6. 等待 1-2 分钟自动构建

7. 访问 https://ai-tools-34x.pages.dev

═══════════════════════════════════════════════════════════════

✅ 成功标志：
- 页面显示 "🚀 Geek工具导航站"
- 能看到分类和工具列表
- 投票功能可用

❌ 如果失败：
- 检查 Deployments 标签的构建日志
- 确认所有字段都填正确了
- 查看是否有错误信息

═══════════════════════════════════════════════════════════════

