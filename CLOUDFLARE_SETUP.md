# Cloudflare Pages 构建配置指南

## 🚀 快速设置

请在 Cloudflare Dashboard 中按以下步骤操作：

### 1. 打开项目设置
- 访问: https://dash.cloudflare.com/
- 导航到: Pages → ai-tools-34x → Settings

### 2. 配置构建设置

在 **Build and deployment** 或 **Builds & deployments** 部分，填入以下信息：

| 字段 | 值 |
|------|-----|
| **Build command** | `npm run build` |
| **Build output directory** | `dist` |
| **Root directory** | `/` |

### 3. 保存并重新部署

- 点击 "Save" 按钮
- Cloudflare Pages 会自动触发新的构建
- 等待构建完成（通常 1-2 分钟）

### 4. 验证部署

- 访问 https://ai-tools-34x.pages.dev
- 应该能看到 Geek 工具导航站的页面

---

## 🔍 如果还是无法显示

### 检查构建日志

1. 在 Cloudflare Dashboard 中
2. 找到 **Deployments** 标签
3. 查看最新构建的日志
4. 检查是否有错误信息

### 常见问题

**问题**: 构建失败
**解决**: 检查日志中的错误，通常是依赖安装问题

**问题**: 页面显示但功能不工作
**解决**: 检查浏览器控制台是否有 JavaScript 错误

**问题**: API 请求失败
**解决**: 确保 D1 数据库已绑定，检查 CORS 配置

---

## 📝 配置文件参考

项目中已包含以下配置文件：

- `wrangler.toml` - Cloudflare Workers 和 Pages 配置
- `package.json` - npm 构建脚本
- `vite.config.js` - Vite 构建配置
- `_routes.json` - API 路由配置

这些文件已经正确配置，只需在 Cloudflare Pages UI 中填入相应的值即可。

