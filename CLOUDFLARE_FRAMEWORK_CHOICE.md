# Cloudflare Pages 框架选择指南

## ❓ 预设框架选择

在 Cloudflare Pages 中连接 GitHub 项目时，会问你选择框架。

### 🎯 正确答案：**选择 "无" 或 "自定义"**

---

## 📋 为什么选择"无"？

| 框架选项 | 说明 | 是否选择 |
|---------|------|--------|
| **无 / None / Custom** | 手动指定构建命令 | ✅ **选这个** |
| React | 预设 React 配置 | ❌ 不选 |
| Vue | 预设 Vue 配置 | ❌ 不选 |
| Next.js | 预设 Next.js 配置 | ❌ 不选 |
| Nuxt | 预设 Nuxt 配置 | ❌ 不选 |

---

## 🔧 选择"无"后的配置步骤

### 1. 框架选择
```
Framework preset: 无 / None / Custom
```

### 2. 构建命令
```
Build command: npm run build
```

### 3. 构建输出目录
```
Build output directory: dist
```

### 4. 根目录（可选）
```
Root directory: /
```

### 5. 环境变量（可选）
```
暂时不需要配置
```

---

## ✅ 为什么这个选择是正确的

1. **我们有 package.json** - npm 依赖已定义
2. **我们有 vite.config.js** - Vite 构建配置已定义
3. **我们有 npm run build 脚本** - 构建命令已定义
4. **输出在 dist/ 目录** - 所有文件都在这里

Cloudflare Pages 只需要执行我们的构建脚本，不需要预设框架。

---

## 🚫 如果选了"React"会怎样？

❌ Cloudflare 会使用预设的 React 配置
❌ 可能覆盖我们的 Vite 配置
❌ 可能导致构建失败或路径错误

---

## 📝 完整的配置流程

```
1. 连接 GitHub 仓库
   ↓
2. 选择分支: main
   ↓
3. 框架选择: 无 / None / Custom
   ↓
4. 构建命令: npm run build
   ↓
5. 构建输出: dist
   ↓
6. 根目录: /
   ↓
7. 保存并部署
   ↓
8. 等待构建完成（1-2 分钟）
   ↓
9. 访问 https://ai-tools-34x.pages.dev
```

---

## 🎯 总结

**框架选择：选"无"或"自定义"**

然后填入：
- Build command: `npm run build`
- Build output directory: `dist`
- Root directory: `/`

这样 Cloudflare Pages 就会使用我们的 Vite 配置来构建项目！

