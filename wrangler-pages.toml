# Cloudflare Pages 配置
# 该文件用于配置 Pages 构建环境

[env.production]
name = "geek-tools"

[build]
command = "npm run build"
cwd = "./"
root = "dist"

[build.upload]
format = "modules"

# 指定 Node.js 版本
[env.production.vars]
NODE_VERSION = "20.19.0"

